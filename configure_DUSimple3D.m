conf = struct;
conf.delta_goal_point = 5;          % Radius of goal point
conf.delta_near = 3;              % Radius for neighboring nodes
conf.max_step = 3;               % Maximum position change when we add a new node to the tree
conf.mode_change_weight = 1;
